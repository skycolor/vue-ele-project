export default {
	getMenuFolding(state){				//获取菜单是否折叠
        return state.isMenuFolding;
    } ,
    getThemeObj(state){				//当前主题对象
        return state.themeObj;
    } ,
    getUsers(state){				//当前主题对象
        return state.users;
    } 
}